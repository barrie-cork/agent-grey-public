# Core Django
Django==5.1.15  # Security: 5.1.13->5.1.15 (GHSA-frmv-pr5f-9mcr Critical, GHSA-qw25-v68c-qjf3 High); stays on 5.1 LTS
djangorestframework==3.16.0

# Database
psycopg2-binary==2.9.10
dj-database-url==2.1.0

# Environment and Configuration
python-decouple==3.8

# Redis and Caching
redis==4.6.0
django-redis==6.0.0

# Celery with Redis support
celery[redis]==5.5.3
flower==2.0.1

# Utilities
python-dateutil==2.8.2
psutil>=6.1.0,<8.0

# Security
django-cors-headers==4.7.0
netaddr==0.10.1  # For ALLOWED_CIDR_NETS support (DigitalOcean health checks)

# Server-Sent Events (SSE) Support
django-eventstream==4.5.1

# Monitoring and Logging
sentry-sdk==2.8.0  # Security: Updated from 1.40.6 (GHSA-g92j-qhmh-64v2)
python-json-logger==3.2.1
structlog==25.4.0
django-structlog==9.1.1

# Circuit Breaker Pattern
pybreaker==1.2.0

# File handling
Pillow==12.2.0  # Security: 10.3.0->12.2.0 (GHSA-whj4-6x5x-4v2j, GHSA-cfh3-3jmp-rvhc, GHSA-pwv6-vv43-88gr High); used via weasyprint (>=9 ok)
whitenoise==6.6.0

# Document Processing and Export
weasyprint==68.1         # PDF generation (upgraded from 62.3 - CVE-2025-68616, pydyf compat fix)
openpyxl==3.1.2          # Excel export
django-cleanup==8.0.0    # Automatic file cleanup
markdown==3.5.1          # Markdown rendering for consensus discussion comments

# API and Serialization
djangorestframework-simplejwt==5.5.1  # Security: Updated from 5.3.1 (GHSA-5vcc-86wm-547q)
django-rest-knox==5.0.2              # Per-device revocable tokens for extension API
drf-spectacular==0.27.0  # OpenAPI schema generation for dual screening APIs
pydantic==2.10.4

# HTTP Requests (for Serper API)
requests==2.32.4  # Security: Updated from 2.31.0 (GHSA-9wx4-h78v-vm56, GHSA-9hjg-9r4m-mvj7)

# Configuration Management
django-constance==4.3.2

# Cloud Storage (DigitalOcean Spaces)
boto3==1.34.0

# Prometheus Metrics Collection (custom registry + /metrics endpoint only)
prometheus-client==0.19.0

# WebSocket support (already defined above)

# Vite asset management (Issue #27: Dynamic asset resolution)
django-vite==3.0.3

# ORM Query Caching
django-cachalot==2.6.3

# Product Analytics
posthog==3.7.4
