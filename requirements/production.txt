# Include base requirements
-r base.txt

# Production Server
gunicorn==22.0.0  # Security: Updated from 21.2.0 (GHSA-w3h3-4rj7-4ph4, GHSA-hc5x-x2vx-497g)
uvicorn[standard]==0.27.1
daphne==4.1.2  # ASGI server for WebSocket support

# Note: Removed colorlog==6.8.0 - using simple console logging for production

# Celery Database Backend
django-celery-results==2.6.0

# Redis for caching and Celery broker
# redis is already included in base.txt (4.6.0)
# django-redis is already included in base.txt (6.0.0)
hiredis==2.3.2  # C parser for improved performance

# Security
django-csp==3.8
django-permissions-policy==4.20.0

# Monitoring
django-prometheus==2.3.1

# Static Files (simplified for DigitalOcean)
whitenoise[brotli]==6.6.0

# Static files and CDN (DigitalOcean Spaces)
django-storages==1.14.2
boto3==1.34.0

# Email
django-anymail==10.3

# Production utilities
django-environ==0.11.2
dj-database-url==2.1.0

# Database - using psycopg[binary] from base.txt
# Note: Removed django-db-pool==0.0.10 as it's incompatible with Django 4.2
# Django 4.2+ has built-in connection pooling via CONN_MAX_AGE setting
